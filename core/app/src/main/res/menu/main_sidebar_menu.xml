<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

  <group
    android:id="@+id/group_main"
    android:checkableBehavior="single">

    <item
      android:id="@+id/nav_project"
      android:checked="true"
      android:icon="@drawable/ic_folder"
      android:title="@string/section_project" />

    <item
      android:id="@+id/nav_sdk"
      android:icon="@drawable/ic_min_sdk"
      android:title="@string/title_sdk_manager" />

    <item
      android:id="@+id/nav_terminal"
      android:icon="@drawable/ic_terminal"
      android:title="@string/title_terminal" />

    <item
      android:id="@+id/nav_settings"
      android:icon="@drawable/ic_settings"
      android:title="@string/nav_settings" />

    <item
      android:id="@+id/nav_about"
      android:icon="@drawable/ic_info"
      android:title="@string/nav_about" />

  </group>

</menu>